@{
    ViewData["Title"] = "Betalen";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="min-h-screen bg-gray-100 py-20 px-6">
    <div class="max-w-xl mx-auto bg-white shadow-xl rounded-lg p-8 text-center">
        <h1 class="text-3xl font-bold mb-4 text-green-600">Even geduld...</h1>
        <p class="text-gray-700 mb-6">Je wordt doorgestuurd naar onze beveiligde betaalpagina van Stripe.</p>
        <div class="animate-bounce text-4xl">💳</div>
    </div>
</section>

@section Scripts {
    <script>
        setTimeout(function () {
            // fallback, should not be needed normally
            location.href = "/";
        }, 10000);
    </script>
}
